import { Seat, DISCOUNTS, WEEKDAYS } from '../types/seats';

export const generateSeats = (): Record<string, Seat[]> => {
  // ... seu código atual da função generateSeats ...
};

export const calculateDiscount = (seats: Seat[], date: Date) => {
  // ... seu código atual da função calculateDiscount ...
};

export const getSeatPrice = (basePrice: number, type: Seat['type']): number => {
  // ... seu código atual da função getSeatPrice ...
}; 